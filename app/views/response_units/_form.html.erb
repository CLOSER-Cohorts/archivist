<%= form_for(@response_unit) do |f| %>
  <% if @response_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@response_unit.errors.count, "error") %> prohibited this response_unit from being saved:</h2>

      <ul>
      <% @response_unit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :label %><br>
    <%= f.text_field :label %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
