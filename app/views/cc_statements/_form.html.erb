<%= form_for(@cc_statement) do |f| %>
  <% if @cc_statement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cc_statement.errors.count, "error") %> prohibited this cc_statement from being saved:</h2>

      <ul>
      <% @cc_statement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :literal %><br>
    <%= f.text_field :literal %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
