<%= form_for(@cc_sequence) do |f| %>
  <% if @cc_sequence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cc_sequence.errors.count, "error") %> prohibited this cc_sequence from being saved:</h2>

      <ul>
      <% @cc_sequence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :literal %><br>
    <%= f.text_field :literal %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
