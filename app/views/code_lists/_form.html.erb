<%= form_for(@code_list) do |f| %>
  <% if @code_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@code_list.errors.count, "error") %> prohibited this code_list from being saved:</h2>

      <ul>
      <% @code_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :label %><br>
    <%= f.text_field :label %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
